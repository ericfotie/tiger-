generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  // On retire la ligne url d'ici, car Prisma 7 la veut dans le config.ts
}

model Project {
  id          String   @id @default(uuid())
  title       String
  client      String
  type        String
  description String
  images      String[]
  stars       Int      @default(0)
  date        DateTime @default(now())
}

model Message {
  id       String   @id @default(uuid())
  name     String
  subject  String
  content  String
  status   String   @default("nouveau")
  date     DateTime @default(now())
  whatsapp String
}